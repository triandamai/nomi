services:
  db:
    image: pgvector/pgvector:pg16
    container_name: open-agent-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          memory: 1G
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    container_name: open-agent-redis
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 300M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  gateway:
    image: ${DOCKERHUB_USERNAME}/nomi-gateway:v0.0.21
    container_name: open-agent-gateway
    restart: unless-stopped
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - JWT_SECRET=${JWT_SECRET}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - APP_URL=${APP_URL}
      - PORT=${PORT_GATEWAY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
      - JINA_API_KEY=${JINA_API_KEY}
      - S3_URL=${S3_URL}
      - S3_SECRET_KEY=${S3_SECRET_KEY}
      - S3_ACCESS_KEY=${S3_ACCESS_KEY}
      - PUBLIC_GATEWAY_URL=${PUBLIC_GATEWAY_URL}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 800M

  channel:
    image: ${DOCKERHUB_USERNAME}/nomi-channel:v0.0.19
    container_name: open-agent-channel
    restart: unless-stopped
    environment:
      - REDIS_URL=${REDIS_URL}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - WHATSAPP_DB_PATH=${WHATSAPP_DB_PATH}
      - PORT=${PORT_CHANNEL}
      - S3_URL=${S3_URL}
      - S3_SECRET_KEY=${S3_SECRET_KEY}
      - S3_ACCESS_KEY=${S3_ACCESS_KEY}
      - PUBLIC_GATEWAY_URL=${PUBLIC_GATEWAY_URL}
    volumes:
      - channel_data:/app/data
    depends_on:
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 500M

  web:
    image: ${DOCKERHUB_USERNAME}/nomi-ui:v0.0.21
    container_name: open-agent-web
    restart: unless-stopped
    environment:
      - PUBLIC_GATEWAY_URL=${PUBLIC_GATEWAY_URL}
      - PUBLIC_CHANNEL_URL=${PUBLIC_CHANNEL_URL}
      - ORIGIN=${APP_URL}
    deploy:
      resources:
        limits:
          memory: 600M

volumes:
  postgres_data:
  channel_data:
